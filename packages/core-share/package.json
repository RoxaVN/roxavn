{
  "name": "@roxavn/core-share",
  "version": "0.1.28",
  "main": "./dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --passWithNoTests --watch",
    "test:cov": "rimraf coverage && jest --passWithNoTests --coverage"
  },
  "license": "UNLICENSED",
  "author": "",
  "devDependencies": {
    "ts-jest": "^27.0.5",
    "ts-node": "^10.2.1",
    "typescript": "^4.3.5"
  },
  "files": [
    "/dist"
  ]
}
